//! # prax — the command-line player
//!
//! The interactive front end (the frozen `app/Main.hs`'s heir): round-robin
//! turns, an autonomous cast, a numbered action menu on the player's turn, and
//! save/resume. Also the non-interactive subcommands `stress`, `check`, `flow`,
//! and `dump-play`. World selection mirrors the frozen CLI (bar default;
//! intrigue/play/dm/feud/audience/village by name).
//!
//! The demo that ends the program (PLAN.md) is playing village and bar here.
//! Wired stage by stage; the entry point is intentionally inert until S8/S9.

fn main() {}
