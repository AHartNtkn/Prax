//! Obligations and their `□`-closure (DEON property 1): `obliged.*` facts and
//! the `obligedClose` operator that lifts a world's rules into their normative
//! twins so an invoked obligation actually binds.
