# S7 slice 2 — intrigue (+ the budget learns to count)   2026-07-20

CONTRACT     IntrigueSpec/CoreSpec/EmotionSpec/BeliefsSpec: 38 labels = 36
             `// H:` + 2 KILLED — EXACT accounting (slice 1 had an off-by-one).
             prax-vocab gains core, emotion, beliefs. First exercise of Call
             routing (BASE-db quirk, first-case/first-binding), desire-driven
             planning, and endings.
DIFFERENTIAL worldshape GREEN; trace clean (25 records); the intrigue-12
             GOLDEN replayed by LOADING the derived file (the repo-wide
             content gate fires on an injected inline copy — reviewer-tested);
             matrix clean at the floor.
THE BUDGET   The reviewer hashed the action sequences and found the 375-seed
LEARNS       sweep replays FOUR distinct walks ~94 times each — "3,025 records"
             overstated distinct work by two orders of magnitude. Slice 1's
             diagnosis had named BRANCHING FACTOR; my §11 amendment encoded
             RECORD COUNT, the metric that grows on replay. Corrected: the
             matrix now reports DISTINCT WALKS and saturates on them. The
             first honest reading is sobering and valuable:
                 feud 1 · bigfeud 1 · intrigue 4 distinct walks.
             Feud's whole random-walk differential was ONE walk repeated —
             its real evidence was the trace and the pins. The number now says
             so instead of hiding it, which is what slices 3-4 need.
QUALITY      Review APPROVE (0 Critical), the strongest slice yet: 6 injected
             mutations, 6 caught by named pins; the implementer volunteered an
             uncomfortable disclosure the reviewer independently confirmed.
             [I1] above; [I2] the guard class appeared a THIRD time, one rung
             deeper (core call sites mirroring frozen pathNames through the
             unchecked helper) — closed, with a STANDING GATE so it cannot
             recur silently. Minors closed.
SUITES       Rust 465 · Haskell 712 · freeze intact · verify.sh green.
FORKS        none. DIVERGENCES: none new.
