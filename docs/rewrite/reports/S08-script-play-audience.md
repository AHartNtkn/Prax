# S8 — Script + Play + Audience                          2026-07-21

CONTRACT     ScriptSpec 34 + Script/JsonSpec 12 = 46 labels, all `// H:`.
             owed:S8 == 0 (the story-collision discharged, row removed). One
             new owed:S9 written loudly (mid-scene save/resume persistence
             symmetry — Persist has no Rust twin until S9). No new vocab —
             [A1]'s dividend: Audience cost a registry entry, not a slice.
THE HEADLINE **CG-1 CLOSED, and the closure survived a direct attack.** The
             carried gap S7 could only record — the v44 supersession with no
             authored-data net at world scale — is netted by a GENUINE
             authored world (`conformance/fixtures/cg1_supersession.json`,
             decoded through decode_script→compile, superseded when a PLAYER
             performs the authored `shield` beat, not by an injected insert).
             Both halves pinned: control (timer fires at boundary 3) AND
             supersession (fact stands, ending never comes). Deleting
             `expiries.remove` produces EXACTLY four workspace REDs incl. this
             fixture; control stays green. Runs through the differential.
             PROGRAM.md's "Open gaps: NONE" is earned — the record does not
             overstate (reviewer-reproduced verbatim). My §5 "inexpressible"
             claim was FALSE; the soundness lens disproved it with a running
             script, and S8 turned the gap into a closure.
RULINGS      [D-C2] the reserved-family names are a WORLD-INDEPENDENT prax-core
             constant (turn, contradiction, scenePatience, currentScene) — a
             non-script world now carries them, matching the frozen checker;
             the runtime door was DELETED (no dual system). [S-C2] duplicate
             JSON keys handled explicitly (aeson first-wins, pinned). [S-C3]
             the memories guard adjudicated on measurement. [R6] dump-play
             MEASURED byte-identical; the encoder builds a sorted-key Value.
DIFFERENTIAL worldshape play/audience/cg1 GREEN; both traces clean (--emit
             state); the falsifiable prediction HELD — play's 3 endings ⇒
             exactly 3 distinct walks, at the floor. THE AUDIENCE FINDING,
             adjudicated not buried: audience yields only TWO walks, both
             `granted` — the design's "both endings appear" prediction is
             declared FALSE with the mechanical cause (randtrace's envoy menu
             is never empty pre-petition, so it always petitions before the
             boundary-5 timeout). A prediction stated to be falsifiable, and
             falsified honestly.
FREEZE       Extended to cover `examples/` (the shipped play.json is both the
             decode fixture and the encode golden); the post-deletion
             successor is a SEMANTIC pin (the file IS the encoder's own
             re-emission) rather than a hash.
QUALITY      Review APPROVE, 0 Critical. Native pins each verified against
             their OWN named mutation (392ee5e). clippy clean, no unsafe.

## §7 CUT-OVER AUDIT (reconciled to as-built; one row per S8 surface)

| surface | net today | survives deletion? | action | done |
|---|---|---|---|---|
| O1 compiled action-label order | worldshape.shape + native pin | native pin: yes | native pin | ✓ |
| O3 setup_of emission order | worldshape.bodies + native pin | native pin: yes | native pin | ✓ |
| O4 beat when-conjunct order | worldshape.bodies + native pin | native pin: yes | native pin | ✓ |
| story clause order (O2) | 4 ScriptSpec pins | yes | none | ✓ |
| story rule name/period | shape + 4 ScriptSpec pins | yes | none | ✓ |
| initial expiries (audience) | shape + the boundary pin | yes | none | ✓ |
| flow_chart output | full-string native pin | yes | full-string pin | ✓ |
| JSON encoder bytes | FILE pin (encoder == examples/play.json) | yes (file survives) | FILE-driven pin | ✓ |
| reserved-families list | native pin (world-INDEPENDENT constant) | yes | native pin [D-C2] | ✓ |
| bake_actor / JSON key-probe / dup-key | native pins | yes | native pins | ✓ |

The design §7 table's "reserved-families = Rust-only field, out of worldshape"
row is SUPERSEDED by [D-C2]: it is a world-independent core constant, natively
pinned, differential-visible via the frozen worldshape's `reserved` key.

SUITES       Rust 844 · Haskell 712 · freeze (incl. examples/) intact · verify.sh green.
FORKS        none. DIVERGENCES: DIV-4 (as_role, from S7). CARRIED GAPS: NONE (CG-1 closed).
