# S4 — Types + Engine + the builder API                2026-07-20

CONTRACT     The widest stage. EngineSpec 24/27 (+2 owed:S6 anchors, build-
             order owed WHOLE to S9 with an independent no-label regression);
             RngSpec 26/26 (the stream pulled forward — stage plan amended in
             writing); GateSpec 7/13 (scanner half owed:S9). ALL 5 owed:S4
             deferrals discharged, rows removed — the machine gate consumed
             them. prax-vocab born (obliged_lift/obliged_close) under the
             panel's C1 no-cycle split: constants in core, operators in vocab.
DIFFERENTIAL engine.json — 8 perform-semantics scenarios pinned by
             OBSERVATION of the frozen engine (spawn opacity + re-spawn,
             ForEach snapshot, Call BASE-db quirk, expiry laws, Roll, ⊥
             collapse), byte-for-byte, regenerated identical. The reviewer
             drove 6 NEW adversarial cases through BOTH engines: identical.
NETS         Flagship reborn on the real engine: view == naive_closure after
             EVERY perform, all three router tiers instrumented-reached
             (direct 6163 / grow 1509 / reclose 2004 per 2048-case run).
             5 mutation REDs (spawn base-read, monotone neg-conjunct, expiry
             cancel, Lehmer constant, excl grounding).
QUALITY      Panel: soundness SOUND (expiries → HashMap, fork-safety
             restated), design FLAWED→fixed pre-implementation (the C1
             crate cycle, Vec seams, whole-label owing, the corpus, the
             rename primitive). Review APPROVE; I1 (uncommitted regression
             seed) + M1 (free Condition constructors — the reviewer's own
             port named the friction) controller-closed at 195 green.
             3 deviations flagged and adjudicated (recompile shape;
             within-lineage build-order reshape avoiding a banned cross-fork
             Sym compare; the ⊥-stop scope traced against frozen).
SUITES       Rust 195 · Haskell 712 · freeze intact · verify.sh green.
FORKS        none. DIVERGENCES: none new.
