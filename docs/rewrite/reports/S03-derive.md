# S3 — Derive + the closed view                        2026-07-20

CONTRACT     DeriveSpec: 10 pins re-expressed + 4 machine-tracked deferrals
             (owed: S4 — the meta-gate now FAILS if an owing stage goes DONE
             with its deferral standing; synthetic-board RED in the gate's
             own tests). Closure semantics: the static fast-path/full-eval
             split, per-position delta seeding (Derive.hs-exact on the
             monotone fragment), ground_head with no string round-trip,
             singleton meet (the direct walk BANKED), name-sorted fresh
             facts (deterministic ⊥ witness).
DIVERGENCE   DIV-1 SHIPPED AS AN ARTIFACT: the frozen deltaJoin's
             incompleteness (cross-predicate aggregate bodies) is pinned
             both ways — Rust == correct, Rust != frozen — with the frozen
             field regenerated byte-identical from the frozen engine. The
             naive closure IS the semantics (spec authority; no user fork —
             fiction-invisible in every shipped world). Plus a recorded
             posture note: ⊥-witness selection is name-order (frozen is
             nub-order; up-to-set equal; no shipped ⊥ exists).
DIFFERENTIAL derive.json 7 worlds (feud 7→16, village 22→34) + NEW kin.json
             (recursive, multi-round) byte-for-byte; div1.json both ways.
QUALITY      Review FIX WAVE → closed: I1 the flagship net's proven blind
             spot (a take(1) seeding mutation stayed green 256 iters) now
             covered by a deterministic delta-chaining pin + a focused
             proptest, BOTH observed RED under the mutation and green after
             revert; I2 deferrals machine-enforced via owed-stage tags read
             against the PROGRAM board (parser loud on drift). Adversarial
             hand-cases (mutual recursion across the split, delta chaining,
             Calc-into-Match) all prod==naive==hand-LFP. clippy clean, no
             unsafe. Implementer + fixer both died unreported (4th, 5th) —
             reviews/controller substituted, everything re-verified.
SUITES       Rust 127 green · Haskell 712 · freeze intact · verify.sh green.
FORKS        none. DIVERGENCES: DIV-1 (see above).
