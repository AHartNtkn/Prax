# S7 slice 4 — village (the last world crosses)         2026-07-20

CONTRACT     249 labels = 247 `// H:` + 2 KILLED across eleven spec files
             (GoldenDriveSpec enters here). ALL SIX remaining owed discharges
             landed — **owed: S7 == 0**. prax-vocab completes with project,
             witness (full), rumor, repute, deceit, persona, debt, coerce,
             blackmail, confession.
DIFFERENTIAL worldshape GREEN; trace clean (43 records); the village-21 golden
             LOADED and a real detector; matrix clean at 300 seeds → 300
             DISTINCT walks / 15,325 records; **the die-seed sweep 400/400
             clean** — the program's only world-scale RNG coverage.
QUALITY      Review FIX WAVE, 0 correctness bugs in the port. Both Criticals
             were the same shape: a BINDING RULE NOT EXECUTED AND ITS
             NON-EXECUTION NOT DISCLOSED — the class §13 existed to stop.
[C1]         **My §13 amendment contained a false premise.** It asserted
             village writes `*.feels.*` through more than one route and made a
             mutation RED binding on that; village has exactly one InsertFor
             route and one Delete route. So NO shipped world nets the v44
             supersession. Struck and corrected in place (§14 records the
             measurement). The fix wave then closed the closable half: a
             WORLD-SCALE pin over the real compiled village_world(), with a
             control and the refresh case pinned beside it — the law's first
             world-scale coverage in the program. The residual gap is
             registered as **CG-1** in a new CARRIED GAPS section of the
             program board, with instrumented evidence (42-turn trace: 100
             expiry-cancel hits, ALL refreshes, zero supersessions; 60 seeds,
             zero) and a forward check proving S8's script worlds cannot reach
             it either (the compiler refuses the authoring shape). Due at S10
             as a stated limit.
[C2]         `endeavor`'s generated label and guard order were netted ONLY by
             worldshape — a frozen-equality diff that EVAPORATES AT CUT-OVER
             ([D-I1] rules it insufficient). The design's "golden-visible"
             claim measured FALSE. Native pins added, verified reddening under
             both of the review's mutations.
SUITES       Rust 764 · Haskell 712 · freeze intact · verify.sh green.
FORKS        none. DIVERGENCES: DIV-4 (as_role). CARRIED GAPS: CG-1.
