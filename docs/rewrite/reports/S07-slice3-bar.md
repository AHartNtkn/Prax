# S7 slice 3 — bar + dm (the widest integration)        2026-07-20

CONTRACT     84 labels = 84 `// H:` + 0 KILLED, exact across seven spec files
             (BarSpec 22, DirectorSpec 4, ReactionsSpec 13, DeonticSpec 29,
             ConversationSpec 6, ArcSpec 4, WitnessSpec 6). THREE owed
             discharges landed with their KILLED rows removed (6 owed:S7
             remain, all slice 4). prax-vocab gains reactions, deontic,
             conversation, arc, witness. Two worlds from one module.
DIFFERENTIAL worldshape bar + dm GREEN; both 40-turn traces clean (41 records
             each); BOTH goldens (bar-12, loop-bar-25) LOADED from the derived
             files and verified real detectors; matrix clean — and the
             distinct-walk metric earns its keep: 300 seeds → 300 DISTINCT
             WALKS on each world (vs feud 1, intrigue 4), confirmed
             independently on the frozen engine.
QUALITY      Review APPROVE, 0 Critical — the strongest slice of the program.
             6 injected mutations on the NEW machinery (schedule firing,
             expiry supersession, reaction spawn, director bound_to), 6
             caught by named pins. DIV-4 (as_role) complete in all three parts
             [S-I6] demanded. [S-I5] resolved as ruled (no unwrap).
HONESTY      Both volunteered disclosures independently reproduced — including
             the unflattering one: **bar exercises expiries but NEVER the v44
             supersession** its own risk row claimed. The design's risk table
             is corrected in place (marked FALSE — MEASURED) and the unnetted
             risk is pushed explicitly into slice 4's row, so the program
             cannot quietly skip it.
THE CLASS    "The report overstates its own basis" recurred a THIRD time (the
             budget column named a stop that never ran; the seed range was
             chosen, not derived). Fixed structurally rather than instance-wise:
             BudgetStop gains the missing case, report blocks now carry the
             invocation that produced them, and a mechanical provenance check
             makes an unlabelled chosen-vs-measured quantity unrenderable.
SUITES       Rust 547 · Haskell 712 · freeze intact · verify.sh green.
FORKS        none. DIVERGENCES: DIV-4 (as_role, adjudicated + pinned).
